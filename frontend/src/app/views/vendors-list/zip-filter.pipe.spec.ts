import { ZipFilterPipe } from './zip-filter.pipe';

describe('ZipFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ZipFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
